You are a senior backend engineer. Fix a production database migration blocker safely.

## 🎯 Problem
Publishing is blocked because production database is missing:

CREATE UNIQUE INDEX uniq_daily_notify
ON line_notify_logs (coach_name, notify_type, schedule_date)

Production also contains duplicate rows that prevent index creation.

---

## ⚠️ CRITICAL SAFETY RULES

- You MUST operate on the PRODUCTION database, not development
- DO NOT copy development database to production
- DO NOT drop tables
- DO NOT truncate data
- All operations must be transactional and reversible

---

## 🧠 TASKS

### 1️⃣ Connect to Production DB

Use the production DATABASE_URL from Replit Deployments.
Confirm connection is NOT development.

---

### 2️⃣ Detect duplicates

Run:

SELECT coach_name, notify_type, schedule_date, COUNT(*)
FROM line_notify_logs
GROUP BY 1,2,3
HAVING COUNT(*) > 1;

---

### 3️⃣ Deduplicate safely

- Keep one row per group (MIN(id))
- Move deleted rows into backup table:
  line_notify_logs_dedup_backup
- Use transaction (BEGIN / COMMIT)

---

### 4️⃣ Create unique index

CREATE UNIQUE INDEX IF NOT EXISTS uniq_daily_notify
ON line_notify_logs (coach_name, notify_type, schedule_date);

---

### 5️⃣ Verify success

Run:

SELECT indexname
FROM pg_indexes
WHERE tablename = 'line_notify_logs'
AND indexname = 'uniq_daily_notify';

AND:

SELECT COUNT(*) FROM (
  SELECT 1 FROM line_notify_logs
  GROUP BY coach_name, notify_type, schedule_date
  HAVING COUNT(*) > 1
) t;

Must return:
- index exists
- duplicates = 0

---

### 6️⃣ DO NOT modify application logic
Only fix database state.

---

## ✅ EXPECTED RESULT

- Duplicates removed safely
- Backup table created
- Unique index exists
- No data loss except duplicates
- Publish can proceed without migration error

---

## 🚨 DO NOT

- DO NOT use "Copy development database to production"
- DO NOT reset or overwrite production database
- DO NOT ignore duplicates